package Components;

public class PressureSensor extends Component{
    //protected Pressure currentpressure;

    /*public PressureSensor() {
        currentpressure = new Pressure();
    }

    public PressureSensor(Pressure p) {
        currentpressure = new Pressure();
        currentpressure = p;
    }

    public void UpdatePressure(Pressure p) {
        currentpressure = p;
    }

    public Pressure getPressureData() {
        return this.currentpressure;
    }*/
}
